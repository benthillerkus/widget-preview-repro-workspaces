name: Dart

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        channel: [stable, master]
        run-in: ["workspace", "app"]
      fail-fast: false
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: ${{ matrix.channel }}
          cache: true

      - run: flutter --version

      - run: flutter pub get

      - run: flutter widget-preview start
        if: ${{ matrix.run-in }} == "workspace"
        timeout-minutes: 2

      - run: flutter widget-preview start
        if: ${{ matrix.run-in }} == "app"
        working-directory: packages/asdf
        timeout-minutes: 2
